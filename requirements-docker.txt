# Transcriptor v2.1.0 — Dependencias para Docker
# Sin PyAudio (micrófono no disponible en contenedores)

# === Dependencias principales ===
customtkinter==5.2.2
SpeechRecognition==3.16.0
pydub==0.25.1
Pillow==10.3.0

# === Whisper (transcripción offline avanzada) ===
faster-whisper==1.1.1

# === OCR ===
pytesseract==0.3.13
pdf2image==1.17.0

# === Utilidades ===
numpy>=1.26.0,<2.0.0
