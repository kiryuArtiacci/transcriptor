# Transcriptor v2.0.0
# Requiere Python 3.10+

# === Dependencias principales ===
customtkinter==5.2.2
SpeechRecognition==3.16.0
pydub==0.25.1
PyAudio==0.2.14
Pillow==10.3.0

# === Whisper (transcripción offline avanzada) ===
faster-whisper==1.1.1

# === OCR ===
pytesseract==0.3.13
pdf2image==1.17.0

# === Diarización (identificación de hablantes) ===
resemblyzer==0.1.3

# === Utilidades ===
numpy>=1.26.0,<2.0.0                              # requerido por faster-whisper
ctranslate2>=4.0.0,<5.0.0                         # requerido por faster-whisper (CPU)
huggingface-hub>=0.20.0                            # requerido por faster-whisper

# === Requisitos externos del sistema ===
# FFmpeg:    https://ffmpeg.org/download.html   (requerido por pydub)
# Tesseract: https://github.com/UB-Mannheim/tesseract/wiki  (requerido por pytesseract)
# Poppler:   https://github.com/oschwartz10612/poppler-windows/releases  (requerido por pdf2image)
